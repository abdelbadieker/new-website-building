import { ComingSoonModule } from '@/components/ComingSoonModule';
import { Users } from 'lucide-react';

export default function CRM() {
  return <ComingSoonModule title="CRM" description="Manage customer relationships, track interactions, and segment your audience. Build lasting connections with every client." icon={Users} />;
}
